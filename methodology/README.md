> Source of truth: `discovery-index.schema.json` in this directory is copied verbatim from
> `https://harpd.com/data/discovery-index.schema.json` on every sync, so it can never drift from
> the data it describes. Human-readable method and field definitions live in `../METHODOLOGY.md`
> and `../DATA_DICTIONARY.md`.
